import Card from "../card/Card1";

const Cards = (id, original_language, title, poster_path) =>{

    <div >
               
    {product.map(p => <Item key={p.id} products={p}/>)}
  
</div>
}

export default Cards;